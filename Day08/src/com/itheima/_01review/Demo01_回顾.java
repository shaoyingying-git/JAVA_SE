package com.itheima._01review;
/*
    API的使用

    Scanner: 键盘录入
        import java.util.Scanner;

        Scanner sc = new Scanner(System.in);

        sc.nextInt(): 获取键盘录入的整数

        sc.nextLine() : 获取键盘录入的字符串

    Random: 随机数
        import java.util.Random;

        Random r = new Random();

        r.nextInt(10) + 1; // 1 ~ 10

    ArrayList集合
        是一个容器, 可以存放不同数据类型的多个元素(可以使用泛型, 存储相同类型的元素), 长度是可变的

    创建对象
        泛型: 用来限定集合中元素的数据类型, 必须是引用数据类型

        ArrayList<泛型> list = new ArrayList<>();

    方法:
        add(元素): 增加
        remove(索引): 删除
        get(索引): 查询
        size(): 获取长度

    遍历:
        for(int i = 0; i < 集合的长度; i++) {
            list.get(i)
        }
 */
public class Demo01_回顾 {
}
